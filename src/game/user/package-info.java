/**
 * Package to include the Player class, which defines individual players, check if
 * each player move is valid, add them to the game board and verify when a player
 * wins. It includes also classes to simulated different games and the interface for
 * user inputs.
 *
 */
package game.user;